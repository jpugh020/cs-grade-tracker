import { FinalExemptPipe } from './final-exempt.pipe';

describe('FinalExemptPipe', () => {
  it('create an instance', () => {
    const pipe = new FinalExemptPipe();
    expect(pipe).toBeTruthy();
  });
});

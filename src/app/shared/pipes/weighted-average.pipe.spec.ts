import { WeightedAveragePipe } from './weighted-average.pipe';

describe('WeightedAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new WeightedAveragePipe();
    expect(pipe).toBeTruthy();
  });
});

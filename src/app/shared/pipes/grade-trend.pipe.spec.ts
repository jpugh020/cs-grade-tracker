import { GradeTrendPipe } from './grade-trend.pipe';

describe('GradeTrendPipe', () => {
  it('create an instance', () => {
    const pipe = new GradeTrendPipe();
    expect(pipe).toBeTruthy();
  });
});
